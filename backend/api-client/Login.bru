meta {
  name: Login
  type: http
  seq: 8
}

post {
  url: {{host}}/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "tiermaster@gmail.com",
    "password": "{{tiermaster_password}}"
  }
}
